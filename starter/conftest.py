"""Root conftest for the starter project.

This file's presence ensures pytest adds this directory (the project
root, which contains the ``starter`` package and ``main.py``) to
``sys.path``, so that ``import starter...`` and ``import main`` resolve
correctly regardless of the directory pytest is invoked from.
"""
